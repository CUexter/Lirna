[extend]
useDefault = true

[allowlist]
description = "Only repository-owned examples and generated hash manifests are excluded."
paths = ['''(^|/)\.env\.example$''', '''^config/coverage-baseline\.json$''']
