# Guide

[Missing document](../missing.md)
[Missing section](#missing-section)

Run `bun run missing` from the repository root.

```bash
npx fixture-cli
```

The old route lives in `client/src/routes`.
